# Architecture Refactor — Issue Log (traceable ledger)

Scope: `data/**` + `sync/**` of `dev.boudy04.taskvault` (TaskVault Android app), 2026-08-29.
Every issue met during the work is listed with symptom → root cause → fix → verification result.
"Verified" always means: fix applied, then `:app:testDebugUnitTest` re-run green (per-step), then a
final forced `--rerun` regression in the committed final state (16 suites / 145 tests / 0 failures).

| ID | Symptom | Root cause | Fix applied | Verification |
|----|---------|-----------|-------------|--------------|
| ISS-01 | Edit-tool exact-match rejection on `OfflineFirstRepositoryTest.kt` (edit #6 "not found") | Assumed test code shape from memory: the assignees-update test uses `fakePendingOps.getAll().last().payload` inline, not a `lastOp` local | Dumped actual bytes with `Select-String -Context`, re-anchored `oldText` to the real text | Edit applied; Step-2 suite green |
| ISS-02 | Edit-tool exact-match rejection on `TaskMappingExtTest.kt` (edit #5 "not found") | Assumed the payload-assigneeIds test was the last in the class; the Step-0 `IN_PROGRESS` pin test follows it | Re-anchored to the pin test's signature | Edit applied; Step-2 suite green |
| ISS-03 | `:app:compileDebugKotlin FAILED` — `Unresolved reference 'TaskDto'` at `SyncEngine.kt:45` (+ cascade of unresolved `id`/`status`) | Old drain code never *named* `TaskDto` (type only inferred via `toDtoWithoutServerId()`), so the file had no import; the rewritten `drain()` references it explicitly | Added `import dev.boudy04.taskvault.data.source.network.TaskDto` | Compile green; Step-2 suite green |
| ISS-04 | `:app:compileDebugUnitTestKotlin FAILED` — 11× "Argument type mismatch: actual String, expected Int?" in `SyncEngineTest` | `payload()` helper dropped its `localId` parameter (now carried by `enqueue(type, localId, dto)`), but call sites still passed the id string positionally into `payload(serverId: Int?)` | Rewrote all 11 call sites to `payload()` / `payload(serverId = n)` | Suite green (Step 2) |
| ISS-05 | `:shared-test:compileDebugKotlin FAILED` — `'updateCompleted' overrides nothing` at `FakeTaskDao.kt:47` | Dead-code removal (Step 5) removed `TaskDao.updateCompleted`, but the zero-caller sweep had covered only `app/src`, not the separate `shared-test` module where the fake overrides it | Removed the override from `shared-test/.../FakeTaskDao.kt` | Suite green (Step 5). Lesson recorded: caller sweeps must include every module |
| ISS-06 | Plan gate (a) "byte-identical queued-op wire format" is unachievable across Step 2 by construction (payload column JSON shape necessarily changes) | Gate written against the wrong artifact — the real contract is the HTTP request body | Gate re-scoped to "identical HTTP request bodies", pinned by two `FakeApi` wire-body canaries added in Step 0 | Canaries pass in every subsequent run |
| ISS-07 | Upgrade-path risk: ops queued by a pre-Step-2 build decode with `id = 0` (legacy `localId`/`serverId` JSON keys are ignored under `ignoreUnknownKeys = true`) | `TaskPayload` field names differ from `TaskDto` field names | **Accepted, documented** (not fixed): legacy UPDATE/STATUS ops fail once via the existing `error("… without serverId")` path; legacy DELETE skips the remote delete. Consistent with the app's existing `fallbackToDestructiveMigration` practice; dev-stage accepted. New-format ops are unaffected | Analysis + `SyncEngineTest` covers all new-format op paths |
| ISS-08 | Behavioral-equivalence note on Step 3: `setStatus` now evaluates `settings.session.first()` even for personal rows (classifier arguments are eager) | Classifier owns the full rule including `isPersonal` | **Accepted** — it is a pure cached DataStore read with no observable behavior; personal-path tests pin zero-op behavior | `createPersonalTask_*` / `personalTask_*` tests pass |
| ISS-09 | Environmental: GitHub MCP connector unavailable (`mcporter` not on PATH); a background verification session vanished between turns | Tooling not installed in this environment / session cleanup | Worked from the local clone with git CLI; re-ran the suite — Gradle's `UP-TO-DATE` SUCCESSFUL proved the interrupted run had passed, then a forced `--rerun` re-executed everything explicitly | Final forced run: 145/145 PASS |
| ISS-10 | Stale KDoc `[TaskPayload]` in `OfflineFirstRepositoryTest` after the shape collapsed | Doc not part of the mechanical rewrite | Updated to `[TaskDto]` | Compile + suite green |
| ISS-11 | Edit-tool overlap rejection on `DefaultTaskRepository.kt` (import edits touched the same `import java.time.Instant` line) plus a placeholder typo caught before landing | Two edits targeted overlapping regions; one `newText` was accidentally left as a comment | Merged the import edits into one; corrected the createTask replacement to `reminderEngine.onDueAtSet(...)` | Edits applied; Step-4 suite green |

## Accepted (not fixed) — with reasons

- **ISS-07**: pre-upgrade queued ops — accepted per existing destructive-migration practice; a migration path would be new scope.
- **ISS-08**: extra cached settings read — no observable behavior change; keeping the full rule inside the classifier is the point of the seam.
- **`IN_PROGRESS` mapping asymmetry** (`ModelMappingExt.kt` — `toExternal()` derives status from the legacy `isCompleted` flag only): pinned by a Step-0 characterization test as-is. Changing it is a product decision, explicitly out of scope under the "no behavior change" constraint.
- **Parallel UI effort**: 15 modified + 8 untracked UI-layer files left exactly as found; never staged by refactor commits.
